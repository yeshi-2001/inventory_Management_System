rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users can read or write any collection
    match /{collection}/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
